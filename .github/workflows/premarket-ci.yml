name: Premarket CI

on:
  push:
    branches:
      - feature/premarket-report
    paths:
      - src/core/trading_calendar.py
      - src/services/premarket_window_service.py
      - src/services/index_technical_level_service.py
      - data_provider/cn_index_daily.py
      - data_provider/base.py
      - data_provider/yfinance_fetcher.py
      - tests/test_premarket_window_service.py
      - tests/test_trading_calendar.py
      - tests/test_index_technical_level_service.py
      - tests/test_cn_index_daily.py
      - tests/test_index_daily_manager.py
      - tests/test_index_daily_fetchers.py
      - .github/workflows/premarket-ci.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: premarket-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install minimal test dependencies
        run: |
          python -m pip install pytest pandas "exchange-calendars>=4.13.0" "fastapi>=0.109.0" httpx requests tenacity python-dotenv fake-useragent

      - name: Run premarket tests
        run: |
          python -m pytest -p no:cacheprovider tests/test_premarket_window_service.py tests/test_trading_calendar.py tests/test_index_technical_level_service.py tests/test_cn_index_daily.py tests/test_index_daily_manager.py tests/test_index_daily_fetchers.py -q
